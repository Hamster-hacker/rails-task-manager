<h1>My Tasks</h1>

<ul style="list-style: none">
  <% @tasks.each do |task| %>
    <li>
      <%= check_box_tag task.id, "completed", task.completed %>
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to "New task", new_task_path  %>
